name: CI

on:
  workflow_dispatch:

jobs:
  commit-from-power-apps:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run a multi-line script
        run: |
          cmd .github/workflows/sync_repo_with_export.bat
          cd .github/workflows
          dir
